module debouncer(
    input button_in, 
    input clk_in, 
    output reg button_out
    );
    
    reg bit0;
    reg bit1;
        
    always @ (posedge clk_in) begin
       bit1 <= bit0;
       bit0 <= button_in; 
       button_out = bit1 & bit0;
    end
   
endmodule